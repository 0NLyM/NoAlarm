<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.NoAlarm" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>

    <style name="Theme.NoAlarm.Ringing" parent="Theme.NoAlarm">
        <item name="android:windowShowWallpaper">false</item>
        <item name="android:windowAnimationStyle">@null</item>
        <!-- Trasparente: la chiusura scopre con un cerchio cio' che c'e' davvero
             dietro (l'app che stava girando prima della sveglia), non una copia
             disegnata da noi. Finche' la sveglia suona il contenuto Compose resta
             comunque opaco, quindi non si vede nulla in trasparenza fino a quel
             momento. -->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>
