# Copia in `keystore.properties` per firmare i build di release in locale.
# Il file vero non e' versionato (vedi .gitignore).
storeFile=noalarm-release.jks
storePassword=
keyAlias=noalarm
keyPassword=
